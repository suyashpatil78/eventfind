import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suyashpatil.eventfind',
  appName: 'eventfind',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
